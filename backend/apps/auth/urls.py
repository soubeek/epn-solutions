"""
URLs pour l'app Auth
Authentification et autorisation
"""

from django.urls import path

app_name = 'auth'

urlpatterns = [
    # TODO: Ajouter les endpoints d'authentification personnalisés si nécessaire
    # Pour l'instant, on utilise les endpoints JWT par défaut (/api/token/)
]
